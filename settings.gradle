pluginManagement {
    repositories {
        maven { url 'http://dl.bintray.com/kotlin/kotlin-eap' }
        maven { url 'https://plugins.gradle.org/m2/' }
        mavenCentral()
    }
}
rootProject.name = 'Generate Builder'

include 'annotation'
include 'processor'
include 'example'

